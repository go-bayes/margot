% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_flip_forests.R
\name{margot_flip_forests_parallel}
\alias{margot_flip_forests_parallel}
\title{Parallel flip of CATE estimates with reproducible RNG and memory guard}
\usage{
margot_flip_forests_parallel(
  model_results,
  flip_outcomes,
  model_prefix = "model_",
  recalc_policy = TRUE,
  parallel_policy = FALSE,
  n_policy_cores = future::availableCores() - 1,
  verbose = TRUE,
  n_cores = future::availableCores() - 1,
  max_size_GB = 2,
  seed = TRUE
)
}
\arguments{
\item{model_results}{A list containing the model results from margot_causal_forest().}

\item{flip_outcomes}{A character vector of outcome variable names for which CATE estimates should be flipped.}

\item{model_prefix}{A character string indicating the prefix used for model names in the results list. Default is "model_".}

\item{recalc_policy}{Logical; if TRUE (default) recalculates policy trees for flipped outcomes.}

\item{parallel_policy}{logical. if TRUE, policy-tree refits are parallelised.}

\item{n_policy_cores}{integer. number of workers for policy-tree refits.}

\item{verbose}{Logical indicating whether to display detailed messages during execution. Default is TRUE.}

\item{max_size_GB}{numeric. globals cap passed to `future.globals.maxSize` (default 2 GB).}

\item{seed}{`TRUE` for automatic parallel-safe RNG, or an integer for deterministic streams.}
}
\description{
Parallel flip of CATE estimates with reproducible RNG and memory guard
}
