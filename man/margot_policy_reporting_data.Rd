% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_policy_evaluation_reporting.R
\name{margot_policy_reporting_data}
\alias{margot_policy_reporting_data}
\title{Bind stored policy estimates to a rule and reporting context}
\usage{
margot_policy_reporting_data(
  tree,
  leaves,
  value,
  context,
  value_context = context,
  reference = NULL,
  display_weights = NULL,
  reference_label = NULL,
  display_weight_id = NULL
)
}
\arguments{
\item{tree}{The stored policy tree whose terminal node identifiers occur in \code{leaves}.}

\item{leaves}{Data frame with \code{node_id}, unique \code{leaf_label}, \code{estimate}, \code{lower}, \code{upper}, \code{interval_type}, \code{interval_level}, \code{interval_method} and \code{unavailable_reason}. Estimates are already on the declared scale and orientation. Every terminal node must appear exactly once. Use numeric \code{NA} for unavailable endpoints.}

\item{value}{One-row data frame with the same estimate and interval fields, plus \code{comparator_id}, \code{comparator_label} and finite non-negative \code{gain_margin}. Supply the resolved analysis margin.}

\item{context}{Named list of character scalars: \code{outcome}, \code{outcome_label}, \code{rule_id}, \code{population_id}, \code{population_label}, \code{scale_id}, \code{scale_label}, \code{orientation} (\code{as_scored} or \code{reversed}), \code{weight_id}, \code{evaluation_mode}, \code{contrast_label} and \code{qualification}. Evaluation modes are \code{independent_fixed_rule}, \code{selected_full_sample}, \code{repeated_learning} and \code{constructed}. Independent evaluation additionally requires distinct \code{development_id} and \code{evaluation_id}. The qualification states the inferential limitations, including any multiplicity adjustment.}

\item{value_context}{Context for D; defaults to \code{context}. A separate rule identity is allowed only for explicitly labelled \code{selected_full_sample} leaves with \code{repeated_learning} value. Outcome, population, scale, orientation and weight identities must agree.}

\item{reference}{Optional complete prediction data frame for A/B. Its rows define the display population, which may differ from the evaluation population. Only tree columns are retained. Supply unique participant rows verified using participant identifiers.}

\item{display_weights}{Optional weights aligned with reference rows. \code{NULL} means equal display weights. A zero-weight record contributes to the unweighted count and has zero weight in the share calculation.}

\item{reference_label}{Character scalar describing the display population; required with \code{reference}.}

\item{display_weight_id}{Character scalar identifying the display weights; required with \code{reference}.}
}
\value{
A \code{margot_policy_reporting_data} list containing unrounded tables, contexts, rule signature and optional reference rows and weights. New plotting and text functions revalidate this object before use.
}
\description{
Validates supplied leaf contrasts and a rule-minus-comparator value difference. Computes only descriptive reference counts and weighted shares when reference rows are supplied. Metadata records caller-supplied provenance. Validation checks internal compatibility. Scientific identification and interval coverage require independent justification.
}
\details{
Available interval types are \code{pointwise} or \code{simultaneous} for independent rules and repeated procedures, \code{nominal_fixed_leaves} for selected full-sample results, and \code{constructed} for illustrative fixtures. A nominal fixed-leaf interval ignores selection. An unavailable interval requires type \code{unavailable} and a reason. Repeated-fold quantiles describe partition variability. Sampling intervals require a method that accounts for participant reuse. The supplied method and qualification appear in plots and text. Reversal metadata labels an already reversed estimate. The supplied numbers are preserved.
}
