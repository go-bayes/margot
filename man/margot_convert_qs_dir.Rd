% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_convert_qs_dir.R
\name{margot_convert_qs_dir}
\alias{margot_convert_qs_dir}
\title{Convert legacy `.qs` files in a directory to `.qs2`}
\usage{
margot_convert_qs_dir(
  dir_path,
  recursive = TRUE,
  compress_level = 4,
  delete_qs = FALSE,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir_path}{Character; directory to scan.}

\item{recursive}{Logical; recurse into subdirectories. Default `TRUE`.}

\item{compress_level}{Integer; zstd compression level for the new `.qs2` files. Default `4`.}

\item{delete_qs}{Logical; if TRUE, delete each `.qs` file after a successful
`.qs2` round-trip read. Default `FALSE`.}

\item{overwrite}{Logical; if a `.qs2` sibling already exists, overwrite it.
Default `FALSE` (skip).}

\item{quiet}{Logical; if TRUE, suppress per-file output. Default `FALSE`.}
}
\value{
Invisibly, a tibble of `path`, `status` (`"converted"`,
  `"skipped"`, `"failed"`), and `message` for every file processed.
}
\description{
Walks `dir_path`, reads every `.qs` file with the legacy `qs` package, and
writes a sibling `.qs2` file using `qs2::qs_save()`. Originals are kept by
default; pass `delete_qs = TRUE` to remove them once the `.qs2` sibling is
written and read-verified.
}
\details{
Useful when upgrading R to a version for which the `qs` package is not yet
installable: convert your archive once on a machine that still has `qs`, and
subsequent reads only need `qs2`. If you are on R 4.6+ where `qs` no longer
builds, use [margot_convert_qs_dir_docker()] instead — it runs this same
conversion inside a `rocker/r-ver:4.5` container.
}
\examples{
\dontrun{
  margot_convert_qs_dir("~/Library/CloudStorage/.../lab-09-cache")
}

}
\seealso{
[margot_convert_qs_dir_docker()] for users on R 4.6+ who can run Docker.
}
