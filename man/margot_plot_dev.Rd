% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_plot_dev.R
\name{margot_plot_dev}
\alias{margot_plot_dev}
\title{Experimental plot: advanced bias/adjustment features (DEV)}
\usage{
margot_plot_dev(
  .data,
  type = c("RD", "RR"),
  order = c("alphabetical", "magnitude_desc", "magnitude_asc", "evaluebound_desc",
    "evaluebound_asc", "custom", "default"),
  custom_order = NULL,
  title_binary = NULL,
  include_coefficients = TRUE,
  standardize_label = c("NZ", "US", "none"),
  e_val_bound_threshold = 1.2,
  adjust = c("none", "bonferroni"),
  alpha = 0.05,
  ...,
  options = list(),
  label_mapping = NULL,
  save_output = FALSE,
  use_timestamp = FALSE,
  base_filename = "margot_plot_output",
  prefix = NULL,
  save_path = here::here("push_mods"),
  original_df = NULL,
  bold_rows = FALSE,
  rename_cols = FALSE,
  col_renames = list(`E-Value` = "E_Value", `E-Value bound` = "E_Val_bound"),
  rename_ate = FALSE,
  rename_evalue = FALSE
)
}
\arguments{
\item{.data}{Data frame of effect estimates and 95% CI (as produced by
upstream helpers, e.g., group_tab()).}

\item{type}{Character: "RD" (difference) or "RR" (ratio).}

\item{order}{Character ordering mode; see stable margot_plot() for options.}

\item{custom_order}{Optional character vector for custom ordering.}

\item{title_binary}{Optional title for binary outcomes.}

\item{include_coefficients}{Logical; if TRUE, prints numeric effect at left margin.}

\item{standardize_label}{One of "NZ", "US", "none"; controls x‑axis label text.}

\item{e_val_bound_threshold}{Numeric threshold for E‑value bound labelling.}

\item{adjust}{Multiplicity correction: "none" or "bonferroni".}

\item{alpha}{Numeric significance level used by multiplicity correction.}

\item{...}{Additional named options merged into `options` (used by label mapping, theming, etc.).}

\item{options}{Named list of plotting/label options (e.g., colours, sizes, transforms).}

\item{label_mapping}{Optional named list for transform_label().}

\item{save_output}{Logical; if TRUE, saves the result list via here_save_qs().}

\item{use_timestamp}{Logical; append timestamp to saved filename.}

\item{base_filename, prefix, save_path}{Save controls passed to here_save_qs().}

\item{original_df}{Optional original data for back‑transform helpers.}

\item{bold_rows}{Logical; bold rows above the E‑value bound threshold.}

\item{rename_cols}{Logical; if TRUE, rename E‑value columns per `col_renames`.}

\item{col_renames}{Named list of column renames.}

\item{rename_ate}{Logical or character; if TRUE/character, rename the effect column.}

\item{rename_evalue}{Logical; if TRUE, rename E‑value column headers to display form.}
}
\value{
A list with `plot`, `interpretation`, and `transformed_table`.
}
\description{
margot_plot_dev() is an experimental, feature‑rich plotting helper that keeps
all the recent bias analysis improvements (e.g., optional multiplicity
correction, flexible E‑value handling, label mapping, table renaming) while
leaving the stable margot_plot() API unchanged. Expect breaking changes in a
future release as we continue to refine RR/E‑value workflows.
}
