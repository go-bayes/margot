% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_blp.R
\name{margot_table_blp}
\alias{margot_table_blp}
\title{Reporting table of best linear projection coefficients}
\usage{
margot_table_blp(blp, outcomes = NULL, terms = NULL, digits = 2)
}
\arguments{
\item{blp}{A `margot_blp` data frame returned by [margot_blp()].}

\item{outcomes}{Optional character vector of outcomes to retain, in the order
given. Defaults to `NULL`, meaning every outcome.}

\item{terms}{Optional character vector of coefficient terms to retain, in the
order given. Defaults to `NULL`, meaning every term.}

\item{digits}{Integer; decimal places for the reported estimate and interval.
Default 2.}
}
\value{
A data frame of class `margot_blp_table` with columns `outcome`,
  `term`, `estimate`, `conf_low`, `conf_high`, `estimate_ci`, and `status`,
  carrying the mandatory relativity sentence as its `caption` attribute.
}
\description{
Formats a [margot_blp()] result as a reporting table of estimates with 95%
confidence intervals. No significance stars and no p-values are produced, by
design.
}
\details{
Every rendered table must carry the relativity sentence attached as the
`caption` attribute of the returned object. It states that the coefficients
are descriptive projections interpretable only relative to the other
covariates in the registered set.
}
\examples{
\dontrun{
blp <- margot_blp(cf)
tbl <- margot_table_blp(blp, digits = 2)
attr(tbl, "caption")
}

}
\seealso{
[margot_blp()], [margot_plot_blp()]
}
