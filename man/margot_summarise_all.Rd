% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_compute_policy_value_helpers.R
\name{margot_summarise_all}
\alias{margot_summarise_all}
\title{summarise ate, rate and policy tests across all outcomes}
\usage{
margot_summarise_all(
  cf_out,
  target = c("AUTOC", "QINI", "both"),
  adjust = c("bonferroni", "holm", "BH", "none"),
  alpha = 0.05
)
}
\arguments{
\item{cf_out}{margot result list.}

\item{target}{character. keep both rate metrics (`"both"`, default) or drop
one: `"AUTOC"` or `"QINI"`.}

\item{adjust}{character. multiplicity adjustment method (see
[stats::p.adjust()]). default "bonferroni".}

\item{alpha}{numeric. confidence level for rate intervals (default 0.05).}
}
\value{
tibble. one row per outcome.
}
\description{
pared‑back copy of the original (pre‑refactor) helper that pulls key
diagnostics – average treatment effect, two rate metrics (AUTOC & QINI) and
the depth‑specific policy *p*‑value – for every outcome in the object.
returns a tibble ready for downstream plotting or reporting.
}
