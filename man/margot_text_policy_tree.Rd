% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_policy_tree_reporting.R
\name{margot_text_policy_tree}
\alias{margot_text_policy_tree}
\title{Generate standard policy-tree reporting text}
\usage{
margot_text_policy_tree(
  source = c("generic", "heldout_cv", "display_tree"),
  include_ci = TRUE,
  include_plot_convention = TRUE,
  collapse = TRUE,
  object = NULL,
  action_names = c(control = "control", treated = "treatment"),
  label_mapping = NULL,
  value_units = "stored outcome units",
  digits = 3L,
  include_definitions = TRUE
)
}
\arguments{
\item{source}{Character. Reporting source to describe.}

\item{include_ci}{Logical. Include the interval-interpretation sentence.}

\item{include_plot_convention}{Logical. Include the two-panel plot sentence.}

\item{collapse}{Logical. If \code{TRUE}, return one character string;
otherwise return a character vector of sentences.}

\item{object}{Optional \code{margot_policy_tree_cv} object. With source
\code{heldout_cv}, append an interpretation of its stored selected-depth
values and leaf summaries. No policy is fitted or re-evaluated.}

\item{action_names}{Named character vector with \code{control} and
\code{treated} labels, in that order of action coding.}

\item{label_mapping}{Optional named labels for outcome identifiers.}

\item{value_units}{Character scalar describing the stored outcome units.}

\item{digits}{Integer from 0 to 8; display precision only.}

\item{include_definitions}{Logical; prepend the general explanatory text.}
}
\value{
A character string or character vector.
}
\description{
Produces cautious stock text for policy-tree reports. The text describes the
reporting convention without making substantive claims about moderators.
}
\details{
Policy-tree reporting separates the selected action learned by the fitted
tree from the signed treatment-control score contrast computed on evaluation
rows. Let \eqn{\Gamma_{ja}} denote the action score for observation \eqn{j}
under action \eqn{a}, let \eqn{L} denote a policy-tree leaf, and let
\eqn{E_L} denote the evaluation observations routed to that leaf. For binary
actions \eqn{C} and \eqn{T}, the reported contrast is
\deqn{
  \Delta_L =
  \frac{\sum_{j \in E_L} w_j\{\Gamma_{jT} - \Gamma_{jC}\}}
       {\sum_{j \in E_L} w_j}.
}
The stored selected action is learned on training observations \eqn{S_L}:
\deqn{
  \pi(L) = \arg\max_{a \in \{C,T\}}
  \frac{\sum_{j \in S_L} w_j \Gamma_{ja}}{\sum_{j \in S_L} w_j}.
}
If every displayed leaf has the same selected action, the tree describes
variation in score-contrast magnitude rather than a selective rule that
changes actions across leaves.
}
