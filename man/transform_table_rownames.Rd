% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_plot.R, R/transform_table_rownames.R
\name{transform_table_rownames}
\alias{transform_table_rownames}
\title{Transform table row names}
\usage{
transform_table_rownames(
  df,
  remove_tx_prefix = TRUE,
  remove_z_suffix = TRUE,
  use_title_case = TRUE,
  remove_underscores = TRUE
)

transform_table_rownames(
  df,
  remove_tx_prefix = TRUE,
  remove_z_suffix = TRUE,
  use_title_case = TRUE,
  remove_underscores = TRUE
)
}
\arguments{
\item{df}{A data frame whose row names are to be transformed.}

\item{remove_tx_prefix}{Logical. If TRUE, removes 't' followed by numbers and underscore from the start of row names.}

\item{remove_z_suffix}{Logical. If TRUE, removes '_z' from the end of row names.}

\item{use_title_case}{Logical. If TRUE, converts row names to title case.}

\item{remove_underscores}{Logical. If TRUE, replaces underscores with spaces in row names.}

\item{label_mapping}{Label mapping list}

\item{options}{Options list}
}
\value{
Transformed data frame

A data frame with transformed row names.
}
\description{
This function transforms the row names of a data frame based on specified criteria
and provides CLI feedback on the changes made.
}
\details{
The function applies the following transformations in order:
1. Removes 't' followed by numbers and underscore from the start (if remove_tx_prefix is TRUE)
2. Removes '_z' from the end (if remove_z_suffix is TRUE)
3. Replaces underscores with spaces (if remove_underscores is TRUE)
4. Converts to title case (if use_title_case is TRUE)

The function provides CLI feedback for each change made and a summary of the transformation process.
}
\examples{
df <- data.frame(x = 1:3, row.names = c("t1_variable_z", "t2_another_var", "last_variable"))
transformed_df <- transform_table_rownames(df)

}
\keyword{internal}
