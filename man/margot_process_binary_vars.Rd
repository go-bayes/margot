% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_process_binary_vars.R
\name{margot_process_binary_vars}
\alias{margot_process_binary_vars}
\title{Process Binary Variables in a Data Frame}
\usage{
margot_process_binary_vars(data, exceptions = character(0), suffix = "_binary")
}
\arguments{
\item{data}{A data frame to process.}

\item{exceptions}{A character vector of column names to exclude from processing.}

\item{suffix}{A string to append to renamed binary variables. Default is "_binary".}
}
\value{
A data frame with processed binary variables.
}
\description{
This function identifies binary variables (both factors and numeric),
converts them to 0/1 format, and renames them. It allows for exceptions
to be specified, and ignores variables whose names already end with the
specified suffix.
}
\examples{
df <- data.frame(
  a = factor(c("yes", "no", "yes")),
  b = c(1, 0, 1),
  c = c("apple", "banana", "apple"),
  d = factor(c("true", "false", "true")),
  e_binary = c(0, 1, 0)
)
processed_df <- margot_process_binary_vars(df, exceptions = "c")

}
