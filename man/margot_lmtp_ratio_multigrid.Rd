% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_lmtp_ratio_multigrid.R
\name{margot_lmtp_ratio_multigrid}
\alias{margot_lmtp_ratio_multigrid}
\title{LMTP density-ratio multi-shift grid (wave x shift)}
\usage{
margot_lmtp_ratio_multigrid(
  x,
  outcome,
  shifts = NULL,
  waves = NULL,
  scale = "log10",
  theme = "classic",
  bins = 40,
  verbose = TRUE,
  label_mapping = NULL
)
}
\arguments{
\item{x}{LMTP run output with `$models` (e.g., result from `margot_lmtp()`),
or a single LMTP model (has `$density_ratios`), or a list of such models.}

\item{outcome}{Character. Outcome name to plot.}

\item{shifts}{Optional character vector of shifts/policies to include.
Accepts full names (e.g., `t5_pwi_z_shift_up`) or cleaned suffixes
(e.g., `shift_up`, `shift_down`, `null`). If NULL, includes all shifts.}

\item{waves}{Optional integer vector of waves to include. If NULL, includes all.}

\item{scale}{Character; `"log10"` (default) or `"linear"` for the x-axis.}

\item{theme}{ggplot2 theme keyword: `"classic"` (default), `"minimal"`,
`"bw"`, `"gray"`, `"light"`, `"dark"`, `"void"`.}

\item{bins}{Integer; histogram bins (default 40).}

\item{verbose}{Logical; emit informative messages.}

\item{label_mapping}{Optional named list for human-friendly labels. When
provided, used via `transform_label()` if available.}
}
\value{
A ggplot object with `facet_grid(rows = vars(wave), cols = vars(shift))`.
}
\description{
Build a facet grid of density-ratio histograms across waves (rows) and
shifts (columns) for a selected outcome from an LMTP run. Each facet
annotates the proportion of zero ratios (censoring mass).
}
