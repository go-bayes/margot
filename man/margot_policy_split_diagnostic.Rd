% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_policy_split_diagnostic.R
\name{margot_policy_split_diagnostic}
\alias{margot_policy_split_diagnostic}
\title{Repeated Split Diagnostic for Policy Trees}
\usage{
margot_policy_split_diagnostic(
  object,
  model_names = NULL,
  depths = c(1L, 2L),
  n_splits = 50L,
  train_proportion = 0.5,
  seed = 12345,
  covariate_mode = c("original", "custom", "add", "all"),
  custom_covariates = NULL,
  exclude_covariates = NULL,
  tree_method = c("fastpolicytree", "policytree"),
  weights = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{A list returned by \code{\link{margot_causal_forest}} with
saved \code{covariates}, \code{not_missing}, and per-model \code{dr_scores}.}

\item{model_names}{Optional model names, with or without the \code{model_}
prefix. Defaults to all models.}

\item{depths}{Integer vector containing 1, 2, or both. Defaults to
\code{c(1L, 2L)}.}

\item{n_splits}{Integer. Number of repeated train/test splits. Defaults to 50.}

\item{train_proportion}{Numeric in (0,1). Fraction assigned to the training
split. Defaults to 0.5.}

\item{seed}{Integer or \code{NULL}. Seed for reproducibility. Defaults to 12345.}

\item{covariate_mode}{Character; one of "original", "custom", "add", or "all".
Defaults to "original".}

\item{custom_covariates}{Optional character vector used with "custom" or "add".}

\item{exclude_covariates}{Optional character vector of exact names or patterns
to exclude from the policy-tree covariates.}

\item{tree_method}{Character; "fastpolicytree" when available, otherwise
"policytree".}

\item{weights}{Optional weights for held-out summaries. Defaults to
\code{object$weights}.}

\item{verbose}{Logical; print progress messages. Defaults to TRUE.}
}
\value{
A tibble with one row per model, split, and depth. The returned object
  has class \code{margot_policy_split_diagnostic} and a \code{summary}
  attribute with mean and standard deviation of key metrics.
}
\description{
Repeatedly fits policy trees on a training split and evaluates their action
summaries on the complementary split. This diagnostic describes instability in
the policy-learning procedure; it is not the displayed full-data tree.
}
