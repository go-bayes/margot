% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/margot_lmtp_estimator_contract.R
\name{margot_lmtp_estimator_spec}
\alias{margot_lmtp_estimator_spec}
\title{Lock the execution settings for a Margot LMTP analysis}
\usage{
margot_lmtp_estimator_spec(
  trt,
  outcomes,
  policies,
  seed,
  baseline = NULL,
  time_vary = NULL,
  cens = NULL,
  compete = NULL,
  outcome_type = c("continuous", "binomial"),
  id = NULL,
  folds = 5L,
  bounds = NULL,
  learner_profile = c("ensemble", "glm"),
  trim = 0.999,
  weight_column = NULL
)
}
\arguments{
\item{trt}{Character vector naming the exposure at each policy node.}

\item{outcomes}{Character vector naming the terminal outcomes.}

\item{policies}{Named logical vector. Each name is a policy arm and each
value is the `mtp` setting passed to `lmtp` for that arm. Continuous shifts,
including a natural-course arm represented by `shift = NULL`, ordinarily
use `TRUE`.}

\item{seed}{Single whole-number estimation seed.}

\item{baseline}{Optional character vector of baseline covariates.}

\item{time_vary}{Optional time-varying covariate specification passed to
`lmtp`.}

\item{cens}{Optional character vector of censoring indicators.}

\item{compete}{Optional character vector of competing-event indicators.}

\item{outcome_type}{Outcome model, `"continuous"` or `"binomial"`.}

\item{id}{Optional participant identifier column.}

\item{folds}{Number of cross-fitting folds.}

\item{bounds}{Optional common outcome bounds passed to `lmtp`.}

\item{learner_profile}{Registered learner profile, `"glm"` or
`"ensemble"`.}

\item{trim}{Pooled density-ratio quantile cap passed to
[lmtp::lmtp_control()].}

\item{weight_column}{Optional data column containing non-negative analysis
weights. The values remain in the analysis data rather than the
specification.}
}
\value{
An object of class `margot_lmtp_estimator_spec`.
}
\description{
`margot_lmtp_estimator_spec()` records the arguments that determine an LMTP
fit and protects them with a content hash. The specification belongs to
Margot and requires no companion package. It deliberately records execution
settings alone: the causal question, causal estimand, identification
assumptions, and policy rationale remain in the study protocol.
}
\details{
The specification can name several terminal outcomes. When it is passed to
[margot_lmtp()] with `reuse_density_ratios = TRUE`, Margot fits each
policy-specific treatment and censoring density-ratio process once and reuses
it across those outcomes.
}
