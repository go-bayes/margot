# Format detailed statistics for a single outcome

Format detailed statistics for a single outcome

## Usage

``` r
format_outcome_details(
  model_id,
  model_name,
  evidence_row,
  rate_results_list,
  qini_results,
  omnibus_results,
  models,
  spend_levels,
  include_qini = TRUE
)
```
