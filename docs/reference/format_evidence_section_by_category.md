# Format evidence section by category

Format evidence section by category

## Usage

``` r
format_evidence_section_by_category(
  evidence_summary,
  category,
  header_text,
  rate_results_list,
  qini_results,
  omnibus_results,
  models,
  spend_levels,
  label_mapping,
  include_qini = TRUE
)
```
