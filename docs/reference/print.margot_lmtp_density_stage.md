# Print a Margot LMTP density-stage result

Print a Margot LMTP density-stage result

## Usage

``` r
# S3 method for class 'margot_lmtp_density_stage'
print(x, ...)
```

## Arguments

- x:

  A \`margot_lmtp_density_stage\` object, returned by \[margot_lmtp()\]
  with \`stages = "density"\`.

- ...:

  Ignored.

## Value

\`x\`, invisibly.
