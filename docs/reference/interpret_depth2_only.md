# Interpret depth-2 only bootstrap results

Interpret depth-2 only bootstrap results

## Usage

``` r
interpret_depth2_only(
  object,
  model_name,
  outcome_name,
  outcome_label,
  metrics,
  n_bootstrap,
  stability_threshold,
  decimal_places,
  format,
  include_theory = TRUE,
  label_mapping = NULL
)
```
