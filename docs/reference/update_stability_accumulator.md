# Update accumulator with tree information for stability analysis

Update accumulator with tree information for stability analysis

## Usage

``` r
update_stability_accumulator(accumulator, tree_info, depth)
```
