# Extract gain value from plot data at specific spend level

Extract gain value from plot data at specific spend level

## Usage

``` r
extract_gain_from_plot_data(qini_data, curve_name, spend_level)
```
