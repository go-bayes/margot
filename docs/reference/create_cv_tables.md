# Create Formatted Tables from CV Results

Internal function to create formatted tables similar to margot_rate()
output for cross-validation results.

## Usage

``` r
create_cv_tables(cv_results, alpha = 0.05)
```

## Arguments

- cv_results:

  Data frame with CV results

- alpha:

  Significance level for highlighting
