# Get bootstrap indices based on variation type

Get bootstrap indices based on variation type

## Usage

``` r
get_bootstrap_indices(
  not_missing,
  vary_type,
  train_prop,
  sample_seed,
  split_seed
)
```
