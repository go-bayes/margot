# Extract essential information from a policy tree

Extract essential information from a policy tree

Extract essential information from a policy tree

## Usage

``` r
extract_tree_info(tree, var_names)

extract_tree_info(tree, var_names)
```
