# Create Database of Measures (DEPRECATED, use \`boilerplate\` package: \<https://go-bayes.github.io/boilerplate/\>)

\`r lifecycle::badge("deprecated")\` This function is deprecated. Please
use \`boilerplate::boilerplate_manage_measures()\` instead. Install the
new package with \`devtools::install_github("go-bayes/boilerplate")\`.

## Usage

``` r
margot_create_database(...)
```

## Arguments

- ...:

  All arguments (ignored)

## Value

A message indicating the function is deprecated.

## Examples

``` r
if (FALSE) { # \dontrun{
# This function is deprecated. Use instead:
# devtools::install_github("go-bayes/boilerplate")
# library(boilerplate)
# boilerplate::boilerplate_manage_measures(...)
} # }
```
