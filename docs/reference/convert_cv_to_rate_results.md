# Convert CV Results to Rate Results Format

Internal function to convert CV results to the format expected by
margot_interpret_heterogeneity.

## Usage

``` r
convert_cv_to_rate_results(cv_results, flipped_outcomes = NULL)
```
