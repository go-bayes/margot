# Compute baseline QINI curve

Compute baseline QINI curve

## Usage

``` r
compute_baseline_qini(
  tau_hat,
  dr_scores,
  weights,
  method = "maq_no_covariates",
  n_bootstrap = 200,
  seed = NULL,
  verbose = TRUE
)
```
