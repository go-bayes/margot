# Compute QINI Curves with Improved DR Scores (Internal)

Compute QINI Curves with Improved DR Scores (Internal)

## Usage

``` r
compute_qini_improved(
  Y,
  W,
  X,
  tau_hat,
  weights = NULL,
  seed = 12345,
  n_bootstrap = 200,
  verbose = TRUE
)
```
