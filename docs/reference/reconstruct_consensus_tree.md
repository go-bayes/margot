# Reconstruct a consensus tree from consensus splits

Reconstruct a consensus tree from consensus splits

Reconstruct a consensus tree from consensus splits

## Usage

``` r
reconstruct_consensus_tree(
  consensus_splits,
  dr_scores,
  covariates,
  depth,
  tree_method = "policytree",
  min_node_size = NULL
)

reconstruct_consensus_tree(
  consensus_splits,
  dr_scores,
  covariates,
  depth,
  tree_method = "policytree",
  min_node_size = NULL
)
```
